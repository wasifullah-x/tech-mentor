# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# AI/ML (Updated for compatibility)
openai==1.10.0
sentence-transformers>=2.3.1
torch>=2.0.0
transformers>=4.37.2

# Vector Database & RAG
# NOTE: ChromaDB removed for local Windows setup simplicity. The app uses an
# in-memory RAG fallback so the API can start without MSVC build tools.

# Data Processing
numpy>=1.24.0
pandas>=2.0.0


# Web Scraping (Optional)
beautifulsoup4==4.12.3
requests==2.31.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Monitoring & Logging
loguru==0.7.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
